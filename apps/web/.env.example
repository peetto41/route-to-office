# apps/web — there is nothing secret here. The frontend needs no API key at all;
# never add the ORS API key or anything from apps/api's .env to this file.

# Base URL the frontend calls for our own API (server-side proxy target / SSR fetch).
API_BASE_URL=http://localhost:3000
WEB_PORT=3001
